'use strict'

document.addEventListener('DOMContentLoaded', () => {
  console.log('🌟 JS loaded and running!')
  // 必要に応じて動的処理をここに追加
})

setTimeout(() => {
  console.log('時間だよ')
}, 20)
